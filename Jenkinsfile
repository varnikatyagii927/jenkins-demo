pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo Hello from shell 🔥'
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
            }
        }
    }
}
